import styled from '../utils/styled'

const Container = styled('article')`
  border-radius: 0;
  box-shadow: none !important;

  padding: 1.5em 1rem 1em;
  margin-bottom: 4rem;
  word-wrap: break-word;
  word-break: break-word;
  border-radius: 0.2em;
  box-shadow: 0 -1px 10px var(--color-shadow-5), 0 1px 4px var(--color-shadow-10), 0 10px 30px var(--color-bg--main-shadow);
  position: relative;
  background: var(--color-bg--surface);
  padding: 2rem;
  display: block;
  padding: 0 !important;
`

export default Container
